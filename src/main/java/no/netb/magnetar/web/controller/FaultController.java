package no.netb.magnetar.web.controller;

import org.thymeleaf.ITemplateEngine;

public class FaultController extends MagnetarController {

    public FaultController(ITemplateEngine templateEngine) {
        super(templateEngine);
    }
}
